# Environment and Credential Setup

As part of AI module setup, the data ingestion pipeline requires basic environment configuration before execution. This includes configuring API credentials for external services used during ingestion, such as OCR providers.

These credentials are required only during the ingestion and data preparation phase and are not used during runtime query processing. Proper configuration ensures that document processing and OCR steps can be executed securely and consistently across environments.
