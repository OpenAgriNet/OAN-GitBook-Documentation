# Operational Notes

* Session IDs are used to maintain conversation continuity across multiple turns.
* Nudge messages are sent during tool execution to maintain user engagement.
* Telephony, STT, and TTS layers can be replaced with equivalent providers without changes to the AI core.
* The same orchestration pattern can be extended to other OAN deployments with minimal configuration changes.\
  <br>
