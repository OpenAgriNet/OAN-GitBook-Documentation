# Similarity Search

Marqo performs cosine similarity-based search, matching query embeddings against ingested document embeddings. The system retrieves the most relevant chunks and passes them to the agent.

When a query related to cotton fertilisation is issued, the vector database retrieves the most semantically similar content chunks from previously ingested advisory documents. The top-ranked results are passed to the agent, along with their source metadata.

<br>
